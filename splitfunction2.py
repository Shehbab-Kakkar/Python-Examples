#!/usr/bin/env python3.6
a,b = [int(x) for x in input("Enter 2 numbers: ").split(',')] #Without argument under the split consider a space as a spliting way
print("Sum: ",a+b)


#Split must use string or none as a splitter not int
